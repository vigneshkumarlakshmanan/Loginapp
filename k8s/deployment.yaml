apiVersion: apps/v1
kind: Deployment
metadata:
  name: Loginapp
  labels:
    app: loginapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loginapp
  template:
    metadata:
      labels:
        app: loginapp
    spec:
      containers:
        - name: frontend
          image: 018134828981.dkr.ecr.us-west-2.amazonaws.com/dev/loginapp:$BUILD_NUMBER
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: Loginapp
spec:
  type: LoadBalancer
  selector:
    app: login
  ports:
    - port: 80
      targetPort: 8080


